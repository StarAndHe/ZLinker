# App Store listing — English (en-US)

> Product name: **ZLinker**. Bundle: `org.songsong.zlinker`.
> Copy is written to App Store Connect field limits (character counts noted).

## App Name (max 30)
```
ZLinker — Remote for ZCode
```
<sub>25 characters</sub>

## Subtitle (max 30)
```
Your ZCode agent, in-pocket
```
<sub>27 characters</sub>

## Promotional Text (max 170)
```
Run your ZCode coding agent from your phone: live tasks, native streaming conversations, scheduled automations, off-peak runs and push notifications — no browser.
```
<sub>159 characters</sub>

## Keywords (max 100, comma-separated, no spaces)
```
zcode,remote,agent,ai,coding,tasks,automation,off-peak,terminal,dev,devops,assistant
```
<sub>84 characters</sub>

## Description (max 4000)
```
ZLinker is a mobile remote for ZCode — your desktop coding agent, in your pocket. It speaks the native protocol, so you get live status, a real task list and full streaming conversations without opening a browser.

NATIVE TASK LIST
See tasks from every device in one place: running state, latest-reply preview and pinned groups. Stop, pause or resume from your phone, then tap a task to jump straight into the conversation.

NATIVE CONVERSATIONS
The full Conversation V4 experience in native widgets: streaming markdown, thinking strips, tool-call summaries with diffs, file-change bars, model / mode / thought switching, queued messages, attachments and history paging.

SCHEDULED AUTOMATIONS
Create, edit and toggle server-side automations from your phone — cron, fixed intervals or one-shot delays. They fire on the desktop even when the app is closed.

OFF-PEAK TASKS
Submit work to run when compute is idle, watch your queue position live and get notified the moment it finishes. (Requires a Coding Plan subscription.)

LOCAL NOTIFICATIONS
Task finished or failed, off-peak result ready, automation fired — pushed straight to your phone. Tap a notification to open the conversation. The web remote can't do this.

MULTI-DEVICE
Add devices by scanning a QR code, pasting a link or decoding a screenshot. Rename, pin, reorder and export encrypted backups. Every device runs its own live connection.

WEB FALLBACK, ALWAYS
If the protocol ever changes, any task still opens in the official web remote. The native path degrades gracefully — the app keeps working.

PRIVACY FIRST
No data collected. No accounts, no relay servers of ours. Device links stay on your device.

ZLinker is an independent, community-built tool and is not affiliated with, endorsed by, or connected to Zhipu AI. Only connect devices you own and use it in accordance with the ZCode terms of service.
```

## What's New (release notes, max 4000)
```
- Native conversations: streaming markdown, tool diffs, model switching, queues and attachments
- Official-mobile-layout task list with phase pills and a tablet dual-pane view
- Scheduled automations and off-peak tasks
- Local push notifications with tap-to-open conversation
- Faster, more reliable multi-device connections
```

## URLs
- Support URL: `https://github.com/opensymph/ZLinker`
- Marketing URL: `https://github.com/opensymph/ZLinker`
- Privacy Policy: `https://privacy.songsong.org/en.html`

## Category & age
- Primary category: Developer Tools
- Secondary category: Productivity
- Age rating: 4+
